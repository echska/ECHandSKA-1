<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>مشاهدة الذكرى | Nafsam</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --accent: #ff3e3e;
            --bg: #080808;
        }

        * {
            box-sizing: border-box;
        }

        body {
            background: var(--bg);
            color: #fff;
            font-family: 'Cairo', sans-serif;
            margin: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            align-items: center;
        }
        
        .top-nav {
            width: 100%;
            padding: 20px 5%;
            display: flex;
            justify-content: flex-start;
        }

        .btn-back { 
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 10px 22px; 
            background: rgba(255,255,255,0.05);
            border: 1px solid rgba(255,62,62,0.3);
            color: #fff;
            text-decoration: none;
            border-radius: 50px;
            font-weight: bold;
            transition: 0.3s;
        }

        .btn-back:hover {
            background: var(--accent);
            transform: translateX(5px);
        }

        .video-section {
            width: 90%;
            max-width: 900px;
            margin-top: 20px;
            padding-bottom: 40px;
        }

        .video-container {
            background: #000;
            border-radius: 20px;
            overflow: hidden;
            border: 1px solid #222;
            box-shadow: 0 20px 50px rgba(0,0,0,0.8);
        }

        video {
            width: 100%;
            display: block;
            max-height: 75vh;
            background: #000;
        }

        .video-meta {
            text-align: center;
            padding: 32px 20px 10px;
        }

        .video-meta h2 {
            color: var(--accent);
            font-size: 0.9rem;
            letter-spacing: 2px;
            margin-bottom: 15px;
        }

        .video-meta p {
            font-size: clamp(1.05rem, 3vw, 1.5rem);
            font-weight: 300;
            color: #eee;
            margin: 0;
            line-height: 1.8;
        }

        @media (max-width: 768px) {
            .top-nav {
                padding: 14px;
            }

            .btn-back {
                padding: 9px 16px;
                font-size: 0.9rem;
            }

            .video-section {
                width: calc(100% - 24px);
                margin-top: 8px;
            }
        }
    </style>
</head>
<body>

    <div class="top-nav">
        <a href="videos.html" class="btn-back">➞ رجوع للأرشيف</a>
    </div>

    <main class="video-section">
        <div class="video-container">
            <video controls autoplay>
                <source src="video1.mp4" type="video/mp4">
                متصفحك لا يدعم تشغيل الفيديو.
            </video>
        </div>

        <div class="video-meta">
            <h2>Sunday, 24 August 2025 at 1:42 AM</h2>
            <p>اول فيديو ارسلته الي</p>
        </div>
    </main>

</body>
</html>